{
  "name": "htn2026-backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.13",
    "@types/node": "^26.6.2",
    "typescript": "~6.0.2",
    "vitest": "^4.1.11",
    "wrangler": "^4.101.0"
  },
  "dependencies": {
    "@sentry/cloudflare": "^10.75.0"
  }
}
